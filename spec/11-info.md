Create a top level Info node.
Read info.json and .env from the documets folder.
Add as folders the top level keys in info.json
Use as <size> the subkey of each key in info.json.
Add as <path> the values of each subkey in info.json
Use as <name> the basename of the <path>
Show when you select a link in the browser
a video player using as source  https://$U:$P@$U.$H/<path>

When you click the  play button add to the current player.
