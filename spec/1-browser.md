create an application
that is basically a custom browser

the size is one quarter of screen,
the entire application
placed on bottom right
of the screen

the ui has:
- a browser bar
- a toolbar placed on the bottom
- a body covering the rest of application area

read the config.json when starting

# browsing bar
it shows the usual browser icon back forth and reload and the current address

in the browssing there is
also a pulldown site elector
pull down with values are
the keys in config.json imder sites

selecting one value it navigate to the corresponding site in config.json

remove the search icon

# toolbar

## button extract

on the bottom there is a button bar with the button "extract"

if I click the button dump
it will search for all the a tags
and print on stdout

all the href starting with the prefix under keu extract in config.json






